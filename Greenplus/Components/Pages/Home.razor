@page "/home"

@inject NavigationManager Navigation

<html>
    @if (!isLogin)
    {
        <div class="sidebar">
            <Greenplus.Components.Layout.NavMenu />
        </div>
    }
    <body>
        <div class="resumo">
            <div class="plantio">
                <div class="d-flex align-items-center">
                    <div class="icon-plantio"></div>
                    
                    <div class="text-resumo">
                        <h6>Plantio</h6>
                        <p>00</p>
                    </div>
                </div>
            </div>

            <div class="colheita">
                <div class="d-flex align-items-center">
                    <div class="icon-colheita"></div>

                    <div class="text-resumo">
                    <h6>Colheita</h6>
                        <p>00</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="pizza">
            <p><strong>Vendas</strong> por Categoria</p>

            @* <img src="~/pizza.jpg"/> *@
        </div>

        <div class="coluna">
            <p>Total de <strong>Vendas</strong></p>

            @* <img src="~/pilar.jpg"/>  *@
        </div>
    </body>
</html>


@code {
    private bool isMenuVisible = false;
    private string menuStyle = "width: 0px;";

    private void ToggleMenu()
    {
        isMenuVisible = !isMenuVisible;
        menuStyle = isMenuVisible ? "left: 0;" : "left: -200px;";
    }

    private bool isLogin;


    protected override void OnInitialized()
    {
        isLogin = false;
    }
}
